<UserControl x:Class="TeacherAssistant.CommonButton.CommonButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeacherAssistant.CommonButton"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             VerticalAlignment="Stretch"
             HorizontalAlignment="Stretch">
    <Grid>
        <Button VerticalContentAlignment="Center" Command="{Binding Action}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Button.Content>
                <TextBlock MinHeight="0" Text="{Binding Text}" TextAlignment="Center" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"></TextBlock>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
