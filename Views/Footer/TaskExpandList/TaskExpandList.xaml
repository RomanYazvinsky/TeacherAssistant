<UserControl x:Class="TeacherAssistant.Footer.TaskExpandList.TaskExpandList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TeacherAssistant.Footer.TaskExpandList"
             mc:Ignorable="d"
             d:DesignHeight="25" d:DesignWidth="800" Grid.ZIndex="99">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="25" />
        </Grid.ColumnDefinitions>
        <ProgressBar Grid.Column="0" HorizontalAlignment="Stretch" Height="10" VerticalAlignment="Top" />
        <Expander Name="Expander" Grid.Column="1" ExpandDirection="Up">
            <Popup IsOpen="{Binding (Expander.IsExpanded), RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Expander}}}"
                   PlacementTarget="{Binding ElementName=Expander}" Placement="Top" StaysOpen="False">
                <Grid>
                    <Menu ItemsSource="{Binding Path=Items}"></Menu>
                </Grid>
            </Popup>
        </Expander>
    </Grid>
</UserControl>